apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: preview-project
  namespace: argocd
spec:
  description: Project for Preview Environments
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
